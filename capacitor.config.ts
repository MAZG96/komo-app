import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'KOMO',
  webDir: 'dist/heroesApp',
  bundledWebRuntime: false
};

export default config;
